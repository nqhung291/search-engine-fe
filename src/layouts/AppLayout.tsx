import { IRoute } from '@types'
import React from 'react'
import routes from 'routes'

let allRoutes: IRoute[] = routes

const AppLayout: React.FC = () => {
  return <div>Layout</div>
}

export default AppLayout
